# Generated by Cursor
# Expanded on the host, then mounted into the sandbox .env.d/.
# CHECK_CONTEXT_FILE must be the sandbox path (not the host pre-script path).
export REPO_FULL_NAME="${REPO_FULL_NAME}"
export PR_NUMBER="${PR_NUMBER}"
export HEAD_SHA="${HEAD_SHA}"
export CHECK_CONTEXT_FILE=/sandbox/workspace/check-context.json
