# Generated by Cursor
# Vertex AI env for Fullsend inference inside the sandbox.
# Credentials JSON is mounted by the harness to /tmp/.gcp-credentials.json.
export CLAUDE_CODE_USE_VERTEX=1
export ANTHROPIC_VERTEX_PROJECT_ID="${ANTHROPIC_VERTEX_PROJECT_ID}"
export CLOUD_ML_REGION="${CLOUD_ML_REGION:-global}"
export GOOGLE_CLOUD_PROJECT="${GOOGLE_CLOUD_PROJECT:-${ANTHROPIC_VERTEX_PROJECT_ID}}"
export GOOGLE_CLOUD_LOCATION="${GOOGLE_CLOUD_LOCATION:-${CLOUD_ML_REGION}}"
export GOOGLE_APPLICATION_CREDENTIALS=/tmp/.gcp-credentials.json
